# 프론트엔드 레이아웃

| 버전 | 일시           | 변경 내용                                                                                                                                                                                                                                                   |
| ---- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v0.1 | 2026.07.16 THU | 최초 작성. 라우트 · 레이아웃 · 헤더/푸터 · 반응형 · 스크롤 구조 분리.                                                                                                                                                                                       |
| v0.2 | 2026.07.17 FRI | 폴더 구조 개편 반영. 일반 화면 레이아웃명 `ProjectLayout` → `AppLayout`.                                                                                                                                                                                    |
| v0.3 | 2026.07.17 FRI | **헤더를 레이아웃이 그리도록 확정.** 레이아웃이 Query·Auth를 직접 읽어 조립. `AppLayout`에 `variant` 도입.                                                                                                                                                  |
| v0.4 | 2026.07.19 SUN | ProjectForm을 **생성/설정 두 화면으로 분리** — `variant` 3종으로 확장. **콘텐츠 컨테이너 규칙 신설**(폭·여백, 브레이크포인트 점프 제거). 설정 화면 뒤로가기 목적지 변경. 404 배치 명시. **공용 조각 책임 분리 흡수**(`02`에서), heading 계층은 `05`로 이관. |

---

## 라우트

| 라우트              | 경로                     | 레이아웃                               |
| ------------------- | ------------------------ | -------------------------------------- |
| **Login**           | `/login`                 | `AuthLayout`                           |
| **Signup**          | `/signup`                | `AuthLayout`                           |
| **Dashboard**       | `/`                      | `AppLayout variant="dashboard"`        |
| **ProjectCreate**   | `/projects/new`          | `AppLayout variant="project-create"`   |
| **ProjectSettings** | `/projects/:id/settings` | `AppLayout variant="project-settings"` |
| **SpecDetail**      | `/projects/:id`          | `SpecLayout`                           |
| **404**             | `*`                      | **없음** (자체 렌더)                   |

멘션 알림 딥링크 (백엔드 확정):
`/projects/:projectId/endpoints/:endpointId?comment=:commentId`
→ 해당 댓글로 스크롤 + 하이라이트. 초대 알림은 프로젝트로만 이동.

**404는 레이아웃 밖이다.** 존재하지 않는 경로라 프로젝트 헤더·UserMenu가 어색하고,
`variant`도 정할 수 없다. 대신 `AuthLayout`과 같은 규칙(가로 중앙, `max-w-sm`)으로 톤을 맞추고
탈출 링크(대시보드로 돌아가기)를 반드시 둔다. 푸터는 렌더한다.

---

## 레이아웃 3종

### 헤더는 레이아웃이 그린다

**레이아웃이 헤더·푸터·골격을 소유하고, 페이지는 콘텐츠(`<Outlet>`)만 채운다.**
헤더가 필요로 하는 데이터는 페이지가 넘겨주지 않고 레이아웃이 직접 읽는다.

- 프로젝트 이름 → `useProject(id)` (`id`는 URL `useParams`에서)
- 로그인 유저·Owner 여부 → `useAuth()`

페이지와 레이아웃이 같은 소스를 각자 읽으므로 프롭 드릴링이 없다.

### AppLayout의 `variant`

| variant            | 헤더 좌측           | 뒤로가기 목적지 |
| ------------------ | ------------------- | --------------- |
| `dashboard`        | "Dashboard" 텍스트  | —               |
| `project-create`   | `←` + "새 프로젝트" | `/`             |
| `project-settings` | `←` + 프로젝트명    | `/projects/:id` |

- 분기는 `id` 유무가 아니라 **`variant`** 로 한다.
- 설정 화면의 뒤로가기는 목록이 아니라 **해당 프로젝트**로 보낸다.
- `⚙`(설정 버튼)은 `SpecLayout` 헤더에만 있다. ProjectSettings는 이미 설정 화면이다.

**variant 분기가 늘어나는 것에 대해**: 화면이 늘면 분기가 하나씩 붙는다. 현 규모(5개 미만)에서는
감당 가능하다. variant가 7~8개를 넘거나 헤더 좌측이 복잡한 UI(검색창·탭·필터)를 갖게 되면
`AppHeaderLeft` 조각으로 격리하거나 페이지가 슬롯을 주입하는 방식으로 전환한다. 그 전에는 하지 않는다.

### SpecDetail을 분리하는 이유

헤더가 아니라 **본문 구조** 때문이다. Dashboard·ProjectForm은 페이지 전체가 스크롤되지만
SpecDetail은 화면 높이에 고정되고 3컬럼이 각자 스크롤한다. 같은 레이아웃에 넣으면
높이 계산과 스크롤 소유권이 조건 분기로 얽힌다. 헤더·푸터는 공유하므로 중복은 없다.

---

## 콘텐츠 컨테이너 규칙

레이아웃은 골격만 갖고, **본문의 폭과 좌우 여백은 페이지가 정한다.**

| 화면                  | 폭          | 여백         |
| --------------------- | ----------- | ------------ |
| Dashboard (목록 있음) | `max-w-5xl` | `px-8 py-6`  |
| Dashboard (빈 상태)   | `max-w-3xl` | `px-8 py-6`  |
| ProjectCreate         | `max-w-3xl` | `px-8 py-6`  |
| ProjectSettings       | `max-w-3xl` | `px-8 py-6`  |
| Login · Signup · 404  | `max-w-sm`  | `p-4` (중앙) |

- **브레이크포인트로 여백을 바꾸지 않는다.** `p-4 sm:p-6`은 639→640px에서 가로·세로가
  동시에 점프해 콘텐츠 시작점이 튄다. 한 값으로 고정한다.
- **중앙 영역 안에서 좌상단부터 시작한다.** 빈 상태도 세로 중앙 정렬을 하지 않는다 —
  목록이 생겼을 때와 같은 지점에서 시작해야 화면 전환이 튀지 않는다.
- Dashboard만 상태에 따라 폭이 다르다(목록=카드 그리드, 빈 상태=텍스트 한 덩어리).
  전환 시 좌측 기준선이 이동하는 것은 감수한 트레이드오프다.

---

## 공용 조각의 책임 분리

| 조각          | 책임                        | 안 하는 것                |
| ------------- | --------------------------- | ------------------------- |
| `PageHeading` | 아이콘 + 제목(`h2`)         | 폭·여백을 정하지 않음     |
| `EmptyState`  | `PageHeading` + action 버튼 | 폭을 정하지 않음          |
| `Header`      | 높이 · sticky · 좌우 배치   | 슬롯 내용을 만들지 않음   |
| `Footer`      | 하단 고정 · 정렬            | 링크를 두지 않음          |
| `Logo`        | 문자열 · 폰트 · 크기 기본값 | 렌더 태그를 고정하지 않음 |

**폭은 페이지가 정한다.** 공용 조각이 `max-w-*`를 들고 있으면 페이지 컨테이너와
이중으로 걸려 좌측 기준선이 어긋난다.

---

## 헤더

`Header`는 `left` · `right` · `wide` 세 슬롯을 받는다. `sticky top-0 z-50`이며
**스크롤 컨테이너 밖**에 두어야 한다(부모에 `overflow`가 있으면 sticky가 죽는다).
보더는 기본으로 없고 `window.scrollY > 60`일 때만 나타난다.

**슬롯을 채우는 것은 레이아웃이다.** 페이지는 `Header`를 건드리지 않는다.
조각들이 어느 폴더에 사는지는 `02-frontend-directories`.

| 위치    | dashboard   | project-create      | project-settings | SpecLayout                    |
| ------- | ----------- | ------------------- | ---------------- | ----------------------------- |
| `left`  | "Dashboard" | `←` + "새 프로젝트" | `←` + 프로젝트명 | `←` + 프로젝트명 + `⚙`(Owner) |
| `right` | UserMenu    | UserMenu            | UserMenu         | Bearer + UserMenu             |
| `wide`  | —           | —                   | —                | Bearer(모바일 2행용)          |

### 반응형

브레이크포인트는 **`md`(768px)** 하나만 쓴다.

```
데스크탑 (≥ md)
┌──────────────────────────────────────────────┐
│ ←  결제 API  ⚙        [Bearer 토큰__]  아바타 │   1행
└──────────────────────────────────────────────┘

모바일 (< md)
┌────────────────────────────┐
│ ←  결제 API  ⚙      아바타 │   1행
│ [Bearer 토큰____________]  │   2행
└────────────────────────────┘
```

- Bearer는 **자리를 옮길 뿐 사라지지 않는다.**
- 구현은 `order` + `flex-wrap`. **DOM을 두 벌 렌더하지 않는다.**
- 그 외 화면 헤더는 요소가 적어 모든 폭에서 1행 유지. 프로젝트명만 `truncate`.

---

## 푸터

하단 고정, 배경 없음, `pointer-events-none`. 아래 요소의 클릭을 막지 않으므로
**링크를 두지 않는다**(클릭이 통과한다). 본문은 32px만큼 `pb`를 확보한다.
`align` prop은 `"left" | "center" | "right"`.

푸터 로고는 장식이므로 `<Logo as="span">`으로 렌더한다(heading 계층은 `05-code-conventions`).

---

## 스크롤 구조

| 레이아웃     | 스크롤 주체                                    |
| ------------ | ---------------------------------------------- |
| `AuthLayout` | 없음 (화면에 들어감)                           |
| `AppLayout`  | **문서 전체** (`min-h-dvh`, window 스크롤)     |
| `SpecLayout` | **각 컬럼** (`h-dvh` 고정, 페이지 스크롤 없음) |

`h-dvh`를 쓰는 이유: `100vh`는 모바일에서 주소창을 포함한 높이라 화면 아래가 잘린다.

---

## SpecDetail 3컬럼

| 컬럼   | 폭              | 스크롤 | 보더                   |
| ------ | --------------- | ------ | ---------------------- |
| 왼쪽   | 240px 고정      | 세로   | **없음**               |
| 중앙   | 남는 폭         | 세로   | —                      |
| 오른쪽 | 300px, 리사이즈 | 세로   | `border-l` (패널 구분) |

- 오른쪽 댓글 패널만 드래그 리사이즈(`react-resizable-panels`). 왼쪽은 고정폭.
- 좁은 화면에서는 **가로 스크롤**을 허용한다(`min-w-[900px]`).

> **와이어프레임 v0.1과의 차이**: 와이어프레임은 양쪽 리사이즈로 그려져 있으나
> 실제 구현은 **왼쪽 고정 + 오른쪽만 리사이즈**다.

---

## 모바일 원칙

**차단 화면("데스크탑에서 이용해 주세요")을 만들지 않는다.** 최악의 UX다.
3컬럼이 안 들어가면 가로 스크롤로 처리한다.
